[project]
name = "threadify-mini-agent"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["opentelemetry-exporter-otlp-proto-http>=1.36,<2"]

[dependency-groups]
dev = ["pytest>=8,<9", "httpx>=0.28,<1"]

[tool.uv]
package = false
