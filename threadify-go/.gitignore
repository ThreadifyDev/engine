bin/
*.log
.env
.DS_Store
coverage.html
# GoReleaser output
/dist/

/bundle/

# Platform agent runtimes are CI release artifacts, not source files.
/agent-release/

# Compiled by CI from the harnest toolchain on every run; never byte-identical
# to a prior commit, so keep them untracked like /agent-release/ above.
/internal/agentbundle/assets/agent.tar.gz
/internal/agentbundle/assets/runtimes.json
