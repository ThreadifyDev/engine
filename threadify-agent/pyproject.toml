[project]
name = "threadify-agent"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
  "graphql-core>=3.2,<4",
  "httpx>=0.28,<1",
  "PyJWT[crypto]>=2.10,<3",
  "PyYAML>=6,<7",
  "openai>=2,<3",
]

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.uv]
package = false
